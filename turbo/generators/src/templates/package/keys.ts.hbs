/**
 * Environment configuration keys for {{packageName}}
 */

import { createEnv } from "@t3-oss/env-core";
import { z } from "zod";

export const keys = createEnv({
  server: {
    // TODO: Add environment variables here
  },
  runtimeEnv: process.env,
  emptyStringAsUndefined: true,
});
