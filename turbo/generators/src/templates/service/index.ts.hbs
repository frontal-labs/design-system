import { Hono } from "hono";
import { keys } from "./keys";

const app = new Hono();

app.get("/health", (c) => c.json({ status: "ok", service: "{{name}}" }));

// TODO: Add service routes and handlers

export default {
  port: 3000,
  fetch: app.fetch,
};

export { app };
export { keys };
