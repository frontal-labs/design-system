import { describe, expect, it } from "vitest";
import { ArrowDownBoxIcon } from "../src/index";

describe("@frontal/icons", () => {
	it("should export icon components", () => {
		expect(ArrowDownBoxIcon).toBeDefined();
	});
});
