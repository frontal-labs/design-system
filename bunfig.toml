[install]
# Use production dependencies by default
production = false

# Auto-install peer dependencies
peer = true

# Don't save exact versions
exact = false

# Use lockfile
lockfile = true

[install.scopes]
# Scope-specific configurations
"@frontal" = { token = "" }

[test]
# Test configuration
# preload = ["./test/setup.ts"]  # Uncomment if test setup file exists

[run]
# Run configuration
# Bun version is specified in package.json packageManager field

