import { describe, expect, it } from "vitest";
import { AccordionPrimitive } from "../src/index";

describe("@frontal/primitives", () => {
	it("should export primitive components", () => {
		expect(AccordionPrimitive).toBeDefined();
	});
});
